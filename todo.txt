

You:
I need to work on my forecasting project tonight

Bot:
I found you’re free from 7:30–9:00pm tonight.

I’d block 90 minutes for “Forecasting project deep work”.
Reply “yes” and I’ll add it to your calendar.




decision time:
    - so every time im sending a message, do i provide my calendar to gemini?
        - i think ideally only provide calendar to gemini when i need to
            - how do i do that?
                - have a prompt that categorises the type of question im asking it
                    - am i asking for motivation
                    - am i asking for it to add something to the calendar for me
                - based off of this
                    - resend another prompt
                    - example:
                        - if its a calendar type prompt
                            - read my current calendar events (problem how much of the calendar event do i read)
                            - chuck this extra info alongside my prompt



1. User profile / preference memory
2. Goal and task memory
3. Conversation memory
4. Behaviour / topic memory
5. Operational memory



goal and task agent:
# add task
# add goal
# remove them if done

based on the ENUM, it should read the appropriate file
chuck the contents of that file and my message into the AI
spit out the new appropriate file contents

if i prompt "i finished working out, i now need to shower"
it should add showering to my tasks and remove working out from the file

it should then say what it has done to me the user on discord

need to handle errors when gemini return some random error. 



1. ChatAgent
   - chat
   - unknown maybe

2. CoachingAgent
   - motivation
   - plan_day
   - reflect

3. CalendarAgent
   - calendar_query
   - schedule_task

4. GoalTaskAgent
   - add_goal
   - add_task
   - mark_task_done

5. ReminderAgent
   - create_reminder

6. MemoryAgent
   - update_memory

7. OperationalAgent
   - approval_response
   - cancel_action


my goal task agent needs:
- user message
- intent result
- current goal/tasks file



if i ask what are my goals, it uses chatagent and no goaltaskagent. but this is the json returned:
    ("Intent JSON intent=<Intent.CHAT: 'chat'> confidence=0.95 "
    'needs_calendar=False needs_goal_memory=True needs_task_memory=True '
    'needs_operational_memory=False needs_conversation_history=True '
    'updates_goal_memory=False updates_task_memory=False '
    'updates_user_memory=False updates_reflection_memory=False '
    'requires_approval=False needs_clarification=False clarifying_question=None '
    'entities=IntentEntities(task_title=None, goal_title=None, date=None, '
    'time_preference=None, duration_minutes=None, reminder_text=None, '
    'completed_task=None)')